// Char, Bool and Unit
// No.2 - Boolean

// Make it work
pub fn bool() {
    let f = false; // change to false to make it work!
    let t = true && false; // false
    assert_eq!(t, f);

    println!("Success!");
}
